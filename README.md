# Бот для Мониторинга Серверов и Конвертации VLESS

Этот Telegram-бот предназначен для мониторинга доступности серверов по ICMP (ping) и конвертации ссылок-подписок VLESS.

## 🚀 Основные возможности

- **Мониторинг серверов:** Периодическая проверка доступности серверов.
- **Уведомления:** Мгновенные оповещения в Telegram при изменении статуса сервера (UP/DOWN).
- **Ручная проверка:** Возможность в любой момент проверить статус конкретного сервера.
- **Управление серверами:** Удобное добавление и удаление серверов для мониторинга.
- **Автоматическое именование:** Если вы добавляете несколько серверов из одной страны, бот автоматически присвоит им уникальные имена (например, `Россия-1`, `Россия-2`).
- **Конвертер VLESS:** Утилита для извлечения ключей из ссылки-подписки VLESS.
- **Настраиваемый интервал:** Возможность изменять частоту проверок мониторинга.
- **Защита доступа:** Ключевые команды защищены паролем администратора.

## 🛠️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <URL вашего репозитория>
cd <папка проекта>
```

### 2. Настройка окружения
Создайте файл `.env` в корневой папке проекта, скопировав содержимое из `.env.example`, и заполните его своими данными.

```env
# Токен вашего Telegram-бота (получите у @BotFather)
TELEGRAM_TOKEN=ВАШ_ТЕЛЕГРАМ_ТОКЕН

# Пароль для доступа к командам администратора
ADMIN_PASSWORD=ВАШ_СЕКРЕТНЫЙ_ПАРОЛЬ

# Максимальное количество одновременных сессий администратора (опционально, по умолчанию 3)
MAX_SESSIONS=3

# Имя файла базы данных (опционально, по умолчанию monitoring_bot.db)
DATABASE_FILE=monitoring_bot.db
```

### 3. Запуск бота
Для первого запуска и автоматической установки всех зависимостей используйте скрипт `start.sh`.

```bash
chmod +x start.sh
./start.sh
```
Скрипт создаст виртуальное окружение, установит необходимые библиотеки, инициализирует базу данных и запустит бота в фоновом режиме. Все логи будут записываться в файл `bot.log`.

### 4. Остановка бота
Для остановки бота используйте скрипт `stop.sh`.

```bash
chmod +x stop.sh
./stop.sh
```

## 🤖 Использование

После запуска бота, начните диалог с ним в Telegram.

### Основные команды
- `/start` - Показать приветственное сообщение.
- `/login <пароль>` - Войти в систему как администратор.
- `/logout` - Выйти из системы.
- `/addserver` - Запустить диалог добавления нового сервера для мониторинга.
- `/removeserver` - Удалить сервер из списка мониторинга.
- `/listservers` - Показать список всех отслеживаемых серверов и их статус.
- `/check` - Запустить диалог для мгновенной проверки статуса сервера.
- `/convert` - Конвертировать ссылку-подписку VLESS.
- `/interval` - Изменить интервал проверок мониторинга.

## 💻 Стек технологий

- **Python 3**
- **python-telegram-bot** - Основная библиотека для работы с Telegram Bot API.
- **SQLite** - Встроенная база данных для хранения списка серверов и администраторов.
- **asyncio** - Для асинхронной обработки задач и неблокирующего мониторинга.
